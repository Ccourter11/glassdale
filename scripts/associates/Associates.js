export const Associate = (associate) => {
  return `
    <section class="associate__card">
        <div class="associate___name">Name of Associate: ${associate.name}</div>
        <div class="associate__associate">Alibi of Associate: ${associate.associate}</div>
    </section>
        `;
};
