export const Criminal = (criminalObj) => {
  return `

    <p class="criminal">
    ${criminalObj.name}
    ${criminalObj.age}
    ${criminalObj.conviction}
    ${criminalObj.dates}
</p>
    
        `;
};
